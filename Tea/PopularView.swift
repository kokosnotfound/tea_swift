import SwiftUI

struct PopularView: View {
    var body: some View {
       Text("Popular Page").padding()
    }
}

struct PopularView_Previews: PreviewProvider {
    static var previews: some View {
        PopularView()
    }
}
